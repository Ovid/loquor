# Hand-authored Markdown (docs, reviews, notes, project instructions) — never
# auto-formatted; Prettier's Markdown reflow fights hand-written prose. `docs/`
# stays listed too, for its non-md walkthrough .txt files.
*.md
docs/

# Vendored, read-only references (also gitignored; belt-and-suspenders).
ifvms.js/
web-llm/
zork1/
zork2/
zork3/
scratch/
.superpowers/

# Vendored Glk layer — pinned verbatim to an upstream SHA; never reformat it.
vendor/

# Captured protocol fixtures (generated output) + their notes — leave as-is.
tests/fixtures/
src/test/zork1.walkthrough.en.json

# Build output and deps.
dist/
node_modules/
package-lock.json
