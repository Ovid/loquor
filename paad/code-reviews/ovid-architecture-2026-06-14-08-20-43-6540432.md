# Agentic Code Review: ovid/architecture

**Date:** 2026-06-14 08:20:43
**Branch:** ovid/architecture -> main
**Commit:** 6540432a637d7c311548d201fe7afbdb0309ff10
**Files changed:** 4 | **Lines changed:** +1059 / -704
**Diff size category:** Large

## Executive Summary

This branch is a single behavior-preserving extraction (F-1): the natural-language
**input** translation engine moves out of the `useNaturalLanguage` React hook into a
new pure-logic module `src/llm/translatePipeline.ts` (`createGenerateRaw`, the now-pure
`runClause`, and `createTranslate`), with the hook reduced to an orchestrator that wires
its refs/setters into the factories. Five specialist reviewers independently performed
line-by-line equivalence checks against the pre-extraction original on `main` and found
**no correctness, error-handling, contract, concurrency, or security regressions**. The
moved code is effectively a verbatim copy; the only behavioral surface is the hook
re-wiring, which preserves ref identity, the `useCallback` dependency set, and the
gate/watchdog semantics. The single issue found is a stale line-count figure in the
architecture report (a Suggestion). Confidence in the "behavior-preserving" claim is high:
84 tests across the three relevant suites pass, `tsc -b` is clean.

## Critical Issues

None found.

## Important Issues

None found.

## Suggestions

- `paad/architecture-reviews/2026-06-14-loquor-architecture-report.md:170` — the F-1 status reason states the hook "dropped from **920 → 322 lines**", but the actual post-extraction file is **281 lines** (`src/llm/useNaturalLanguage.ts`), with the extracted engine at 827 lines (`src/llm/translatePipeline.ts`). Documentation-only inaccuracy; no code impact. (Flagged by Concurrency & Contract specialists; verified directly.)

## Plan Alignment

The only design/plan artifact in the diff is the architecture report's own status update.

- **Implemented:** F-1 ("a single 1042-line hook owns the entire NL pipeline") is addressed exactly as the report's status section describes — the `translate` god-callback, `runClause`, the watchdog/generate wrapper, the F-A drain, the `WatchdogTimeout`/`Stage`/`Lex`/`LiveState`/`QueuedLine` types and `MAX_CLAUSES`/`QUEUE_CAP`/`LOAD_WATCHDOG_MS` constants are all moved to `translatePipeline.ts`. Dependency graph stays acyclic (translatePipeline imports only the `Internal` _type_ from useModelDownload).
- **Not yet implemented:** N/A — this branch is scoped to F-1 only. Other architecture-report findings (e.g. F-3 output-translation decomposition, the unpinned-WASM offline gap) remain open by design and are out of scope here.
- **Deviations:** Only the line-count figure in the status reason (see Suggestions). The substantive claims (behavior-preserving, acyclic graph, new `translatePipeline.test.ts` unit-testing `runClause` per stage) all hold.

## Review Metadata

- **Agents dispatched:**
  - Logic & Correctness — behavioral equivalence, closure capture, useCallback deps
  - Error Handling & Edge Cases — error/catch/finally branches, watchdog/timer cleanup, abstain/truncation policy, external-output string matching
  - Contract & Integration — TranslateDeps/ClauseDeps/GenerateRawDeps wiring, public API stability, QueuedLine re-export, import-cycle check, duplication
  - Concurrency & State — ref sharing, translatingRef guard, liveRef freshness ([N]), epoch ([O]), turn-boundary ordering, engineGate priority
  - Security — player-input logging (nlDebug gate, console.error sites), notice interpolation/XSS, prompt injection seam
- **Scope:** changed files (`src/llm/translatePipeline.ts`, `src/llm/useNaturalLanguage.ts`, `src/llm/translatePipeline.test.ts`, architecture report) + adjacent: `/tmp/useNL_main.ts` (the `main` original), `src/ui/Terminal.tsx` (consumer), `src/llm/{types,useModelDownload,guardedGenerate,translate,prompt,engineGate}.ts`
- **Raw findings:** 1 (line-count discrepancy; raised by 2 specialists). Zero code findings ≥ 60 confidence from any specialist.
- **Verified findings:** 1 (Suggestion, verified directly: actual 281 lines vs. claimed 322).
- **Filtered out:** 0 (no specialist produced a ≥60-confidence code finding to filter)
- **Steering files consulted:** `CLAUDE.md` (no code-relevant contradiction; the "NL layer under active refinement" prose lags the report's Fixed status — informational only)
- **Plan/design docs consulted:** `paad/architecture-reviews/2026-06-14-loquor-architecture-report.md` (F-1 status section)

### Note on verification

Because all five specialists independently reached zero code findings via line-by-line
comparison against the `main` original, there were no candidate bug reports for a separate
Verifier pass to confirm or reject. The lone non-code finding (the line-count figure) was
verified directly against `wc -l` output rather than dispatched, and the equivalence claim
was cross-checked by running the suites (84/84 pass).
